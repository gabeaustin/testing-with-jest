const functions = {
    // arrow functions
    // add: (num1, num2) => num1 + num2,

    // without arrow functions
    add: function(num1, num2) {
        return num1 + num2
    }
}

module.exports = functions;